# Task 4.1: Project Setup

## Description

Initialize TypeScript project for the shared config package.

## Requirements

- `package.json` with `smol-toml` pinned dependency, build and test scripts
- `tsconfig.json` configured for Node.js (matching existing packages)
- Folder structure: `src/`, `tests/`
- Added to root `package.json` workspaces array
- Project builds without errors (`npm run build`)

## Dependencies

- None
