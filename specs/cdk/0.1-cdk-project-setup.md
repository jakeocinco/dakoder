# Task 0.1: CDK Project Setup

## Description

Initialize CDK TypeScript project for Dakoder infrastructure.

## Requirements

- `cdk.json` and `package.json` configured
- TypeScript CDK app entry point
- Stack accepts a `stackTag` parameter used to prefix all resource names
- Project synthesizes without errors

## Inputs

- None (greenfield)

## Outputs

- Working CDK project that synths cleanly

## Dependencies

- None (first task)
