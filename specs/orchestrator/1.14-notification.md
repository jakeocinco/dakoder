# Task 1.14: Completion Notification

## Description

Send a notification when a task completes or halts.

## Requirements

- Sends notification on task success (with PR link)
- Sends notification on task failure (with reason)
- Notification mechanism configurable (SNS, Slack webhook, or similar)
- Notification target configured via environment variable

## Dependencies

- 1.9
