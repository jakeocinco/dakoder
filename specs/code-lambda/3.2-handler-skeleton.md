# Task 3.2: Handler Skeleton

## Description

Lambda handler that validates the incoming event and routes to main logic.

## Requirements

- Exports a Lambda handler matching AWS Lambda signature
- Validates `{ taskId, prompt }` — rejects malformed events with 400
- Routes to agent invocation flow

## Dependencies

- 3.1
