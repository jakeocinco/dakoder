# Task 2.1: Project Setup

## Description

Initialize TypeScript project for the Build Lambda.

## Requirements

- `package.json` with build and test scripts
- `tsconfig.json` configured for Node.js Lambda runtime
- Dependencies: `@aws-sdk/client-s3`, `@aws-sdk/client-codebuild`, `@aws-sdk/client-lambda`
- DevDependencies: `@types/aws-lambda`, `@types/node`, `typescript`
- Folder structure: `src/`, `tests/`
- Project builds without errors

## Dependencies

- None
