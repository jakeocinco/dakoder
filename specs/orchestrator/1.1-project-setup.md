# Task 1.1: Project Setup

## Description

Initialize TypeScript project for the orchestrator Lambda.

## Requirements

- `package.json` exists with build and test scripts
- `tsconfig.json` configured for Node.js Lambda runtime
- Dependencies installed: `@aws-sdk/client-s3`, `@aws-sdk/client-lambda`, `@types/aws-lambda`
- Folder structure: `src/`, `tests/`
- Project builds without errors

## Inputs

- None (greenfield)

## Outputs

- Working TypeScript project that compiles cleanly

## Dependencies

- None (first task)
